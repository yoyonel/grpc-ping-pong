if: tag IS blank

dist: xenial   # required for Python >= 3.7
language: python
python:
- '3.7'

install:
  - pip install -U tox-travis

script:
  - tox

after_success:
    - bash <(curl -s https://codecov.io/bash)
